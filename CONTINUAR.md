# Continuar — Movi Import / web Lovable

**Ubicación de este archivo:** raíz del repositorio (`CONTINUAR.md`).

## Retomar en Cursor

En un chat nuevo escribí **`IMPORTACION`** o **`MOV-WEB-LOVABLE`** y pedí seguir.

## Detalle guardado (contactos, negocio, palabras clave)

Todo está en la carpeta **`movi import`**:

- `movi import/referencia-proyecto-web-movi.txt`

---

*Si este archivo no es el que querías (otro nombre, otra carpeta u otro contenido), decime en una frase cómo debe ser y lo ajusto.*
